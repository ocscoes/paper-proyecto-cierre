# Metodología

## Datos

La principal fuente de datos para este estudio es la *AmericasBarometer* del *Latin American Public Opinion Project* (LAPOP Lab), también conocida como Encuesta LAPOP. La encuesta tiene por objetivo recolectar datos la opinión pública sobre democracia y gobernanza en el continente americano. El diseño de la encuesta es probalístico y representativo de la población adulta de cada país [@lapoplab_americasbarometer_2023].

La encuesta se ha realizado de manera regular desde el año 2004. A la fecha, se han realizado 9 olas que han incluido entre 11 a 23 países. El cuestionario se administra a través de encuestas cara  a cara, con la excepción de Canadá y Estados Unidos.  

Complementariamente, se utilizaron indicadores del estudio Latinobarómetro. El estudio declara como objetivo el investigar el desarrollo de la democracia, la economía y la sociedad a partir de indicadores de opinión pública que miden actitudes, valores y comportamientos de la población. Se basa en muestras nacionales de 18 países de América Latina, representativas de la población mayor de 18 años de los respectivos países. 

Como criterio, se incluyeron en este estudio únicamente los países de la región que tengan disponible datos para los indicadores principales del estudio en al menos 5 puntos en el tiempo. De tal manera, esta investigación incluye a 23 de las 33 naciones independientes de América Latina y el Caribe. Como se resumen en @tbl-paises, 11 países tienen datos para todas las olas, y otros 7 tienen datos para 8 olas. 

```{r}
#| label: tbl-paises
#| tbl-cap: Disponibilidad de datos por olas y países

load("input/proc/datos-completos.rdata")

resumen <- datos_wide |>
  dplyr::filter(!is.na(`Cohesión general`), !is.na(ola)) |>
  dplyr::distinct(pais, ola) |>                      # evita duplicados país-año
  dplyr::group_by(pais) |>
  dplyr::summarise(
    n_olas = n(),
    `olas disponibles` = paste(sort(as.integer(ola)), collapse = "; "),
    .groups = "drop"
  ) |>
  dplyr::filter(n_olas >= 5, !pais %in% c("Canada", "United States")) |>
  arrange(desc(n_olas), pais) |>
  select(pais, n_olas, "olas disponibles")

resumen |>
  kable(
    col.names = c("País", "Nº de olas", "Olas disponibles"),
    align = c("l", "c", "l")
  ) |>
  kable_styling(full_width = FALSE, position = "center")
```

Para datos contexuales de los países, se recurrió a diversas fuentes de datos que incluyen:

1) Los datos abiertos del Banco Mundial. Incluye diversos indicadores sobre desarrollo social y económico de la mayoría de los países del mundo. El portal es de datos es accesible en: [https://datos.bancomundial.org/](https://datos.bancomundial.org/).

2) *The Worlwide Governance Indicators* del Banco Mundial. Se trata de una encuesta a expertos que recopila datos primeros sobre diveros indicadores de gobernanza, cubriendo múltiple países con información actualizada entre 1996 y 2003. Los datos son accesibles en: [https://www.worldbank.org/en/publication/worldwide-governance-indicators](https://www.worldbank.org/en/publication/worldwide-governance-indicators).

3) *The V-Dem Dataset* que recoge un conjunto multidimensional de datos que busca medir la calidad de la democracia alrededor del mundo. La base de datos es accesible mediante el paquete de R `vdemdata` [@maerz_vdemdata_2025]

## Variables

### Variables Dependientes

```{r}
#| include: false
library(psych)

datos_subset <- datos_wide |>
  select(`Cohesión vertical`,
         `Cohesión horizontal`)

alpha(datos_subset)
```


Se construyó un **índice de Cohesión Social** ($\alpha$ = 0.74) que se compone de dos dimensiones que, a su vez, son índices sumativos construidos a partir de indicadores de LAPOP y Latinobarometro. El detalle de la construcción de los índices se pueden encontrar aquí: [https://ocscoes.github.io/medicion-cohesion-LA/](https://ocscoes.github.io/medicion-cohesion-LA/).

El **Índice de Cohesión Horizontal** ($\alpha$ = 0.7) se compone de dos subdimensiones, *Seguridad Urbana* y *Confianza Interpersonal*. Seguridad urbana integra indicadores de seguridad objetiva y seguridad subjetiva. Confianza Interpersonal, en tanto, es un único indicador sobre que tan confiuable son las personas en general. Aunque conceptualmente se entiende que el sentido de pertenencia es un componente importante para la cohesión horizontal, ni LAPOP ni Latinobarómetro cuentan con un indicador que permita medir ese fenómeno de manera adecuada. Por otro lado, si bien se esperaba que la participación comunitaria fuera parte del índice, un análisis factorial exploratorio indicó que no forma parte del mismo concepto latente que las otras dos subsimensiones.

El **Índice de Cohesión Vertical** ($\alpha$ = 0.66) se compone de tres dimensiones: *Confianza en instituciones*, *Actitudes hacia la democracia* y *Justicia Distributiva*. Confianza en las instituciones integra indicadores relativos a la confianza de los ciudadananos en el congreso, en el poder judicial y en los partidos políticos. Actitudes hacia la democracia se compone de dos indicadores sobre apoyo al sistema democrático y la satisfacción con el funcionamiento de la democracia en su país. Por último, Justicia distributiva se compone de un único indicador sobre la importancia de que el Estado redistribuya los ingresos en el país. La @tbl-indicadores describe los indicadores seleccionados, así como los índices de cohesión general, horizontal y vertical. 

```{r}
#| label: tbl-indicadores
#| tbl-cap: Resumen indicadores de Cohesión Social

library(summarytools)
library(dplyr)
library(haven)


datos_cohesion <- datos_wide |>
  select(4:11)

# data <- data %>%
#   mutate(across(where(~inherits(.x, "haven_labelled")), as_factor))

print(dfSummary(datos_cohesion,
          headings=FALSE,
          style = "multiline",
          plain.ascii = FALSE,
          tmp.img.dir = 'tmp',
          varnumbers = FALSE,
          silent= TRUE,
          split.by = NULL),
      max.tbl.height = 500,
      method = 'render',
      # css.class = 'table',
      silent = TRUE,
      footnote = NA)
```
### Variables Independientes

*Prosperidad económica* se medirá a través del PIB per cápita a valores de paridad de poder adquisitivo (PPA). *Desigualdad económica* en tanto se medirá a partir del Índice Gini. Ambos indicadores se extrajeron del banco de datos del Banco Mundial.

En cuanto a factores institucionales, se incluyó el Índice de Democracia Electoral, o poliarquía, para medir la *Calidad democrática de los países*. Por otro lado, se incluyó **un índice de gobernanza** ($\alpha$ = X) calculado a partir de los *Worldwide Governace Indicators* del Banco Mundial. 

Por último, se medirá **diversidad cultural** como el porcentaje de población migrante sobre la población total del país, usando datos del Banco Mundial. Dado que la serie está disponible cada cinco año, se construyó una serie anual imputando los años intermedios mediante una interpolación logistica.

## Método

Reconociendo la estructura jerárquica de los datos disponibles (olas anidadas en países), seguimos una estrategia de modelamiento multinivel longitudinal [@singer_applied_2003].  Dado que las observaciones repetidas en el tiempo dentro un mismo país comparten varianza, éstas deben modelarse para evitar inferencias sesgadas producto de considerar cada observación como independiente. La ventaja de los modelos de regresión multinivel radica en que, al modelar efectos aleatorios, capturan correctamente la varianza asociada a la estructura anidada de los datos. De tal forma, es posible captar la heterogeneidad en las trayectorias de cohesión social entre países. Los modelos fueron estimados usando el paquete de R `lme4` (bates_fitting_2015).

El modelo propuesto se podría expresar formalmente como:

$$
y_{ij} = \gamma_{00} + Ola_{ij} + PIB_{ij} + Gini_{ij} + VDem_{ij} + WGI_{ij} + Mig_{ij} + \mu_{0j} + \epsilon_{ij}
$$
Donde,

- $y_{ij}$ es el valor de mediciones repetidas sobre los niveles de cohesión social en los países

- $Ola$ es la medición de tiempo

- $PIB$ es el PIB per capita (PPA) del país

- $Gini$ es el la desigualdad de ingresos del país, medida a través del Índice Gini

- $VDem$ es el nivel de democracia electoral

- $WGI$ es el nivel de gobernanza

- $Mig$ es el porcentaje de población migrante sobre la población total del país

- $\mu_{0j}$ y $\epsilon_{ij}$ son los errores a nivel del país y a nivel de la ola, respectivamente. 




